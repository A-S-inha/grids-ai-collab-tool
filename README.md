# grids-ai-collab-tool

Next.js app in `frontend/` (landing at `/`, app UI at `/dashboard`).

```bash
cd frontend
npm install
npm run dev
```

Or from the repo root: `npm run dev` (uses `frontend` via npm `--prefix`).

Open [http://localhost:3000](http://localhost:3000).
